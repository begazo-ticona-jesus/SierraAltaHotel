---
import CarouselImage from "./widgets/CarouselImage.astro";

const title = "Hotel Sierra Alta";
const description = `Ofrecemos cómodas habitaciones con vistas a la ciudad. Cuenta con terraza, servicio de habitaciones y conserjería. Los huéspedes pueden disfrutar de un desayuno continental y de atención en español e inglés en la recepción. Ubicado cerca de la plaza de armas y a pocos minutos de los Baños Termales La Calera, es una opción ideal para quienes buscan relajarse en el corazón del valle del Colca.`;
---

<section id="init" class="overflow-hidden">
  <div
    class="absolute top-0 left-0 w-full h-full bg-cover bg-center bg-no-repeat sm:bg-cover"
    style="background-image: url('/images/background.webp');"
  >
  </div>
  <div class="relative w-full">
    <div
      class="relative flex flex-col items-center justify-center min-h-[80vh] sm:min-h-[600px] px-4"
    >
      <div
        class="flex flex-col sm:flex-row max-w-5xl w-full mx-auto items-center justify-center"
      >
        <div
          class="flex flex-col items-center w-full max-w-[95%] sm:max-w-2xl lg:max-w-3xl px-6 sm:px-12"
        >
          <h1
            class="text-2xl sm:text-4xl lg:text-5xl text-gray-50 font-black text-center"
          >
            {title}
          </h1>
          <p
            class="mt-4 text-sm sm:text-base lg:text-lg text-gray-100 font-bold text-center"
          >
            {description}
          </p>
        </div>

        <div
          class="flex flex-col items-center w-full max-w-[90%] sm:max-w-lg lg:max-w-2xl px-6 py-8 sm:px-8"
        >
          <div
            class="flex flex-col items-center bg-gray-50 rounded-2xl px-6 py-10 shadow-lg"
          >
            <h1
              class="text-lg sm:text-2xl lg:text-3xl text-color-primary-shadow font-semibold"
            >
              Reservar habitaciones
            </h1>
            <form
              id="contactForm"
              class="flex flex-col items-center mt-4 space-y-4 w-full max-w-xl py-10 px-6"
            >
              <input
                type="text"
                id="name"
                class="w-full border border-gray-300 p-3 sm:p-4 rounded"
                placeholder="Nombre"
                required
              />
              <input
                type="tel"
                id="room"
                class="w-full border border-gray-300 p-3 sm:p-4 rounded"
                placeholder="Tipo de habitación"
                required
              />
              <input
                type="date"
                id="date"
                class="w-full border border-gray-300 p-3 sm:p-4 rounded"
                required
              />
              <button
                type="button"
                onclick="sendWhatsAppMessage()"
                class="bg-color-primary hover:bg-color-primary-shadow text-gray-50 font-bold py-3 sm:py-4 px-8 sm:px-12 rounded"
              >
                Reservar
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
    <CarouselImage />
  </div>
</section>

<script defer src="/src/components/utils/sendMessage.js"></script>
