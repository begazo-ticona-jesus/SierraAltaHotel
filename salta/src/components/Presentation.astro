---
import CarouselImage from "./widgets/CarouselImage.astro";
---
<section id="init">
  <div
  class="absolute top-0 left-0 w-full h-full bg-cover bg-center bg-no-repeat"
  style="background-image: url('/images/background.webp'); background-size: cover;"
  ></div>
  <div class="relative w-full">
    <div class="relative flex flex-col items-center justify-center h-[600px]">
      <div
        class="flex flex-row max-w-5xl w-full h-full mx-auto items-center justify-center"
      >
        <div class="flex flex-col items-center max-w-xl px-10">
          <h1 class="text-5xl text-gray-50 font-black">Hotel Sierra Alta</h1>
          <p class="mt-4 text-lg text-gray-100 font-bold">
            Ofrecemos cómodas habitaciones con vistas a la ciudad. Cuenta con
            terraza, servicio de habitaciones y conserjería. Los huéspedes pueden
            disfrutar de un desayuno continental y de atención en español e inglés
            en la recepción. Ubicado cerca de la plaza de armas y a pocos minutos
            de los Baños Termales La Calera, es una opción ideal para quienes
            buscan relajarse en el corazón del valle del Colca.
          </p>
        </div>

        <div class="flex flex-col items-center max-w-lg px-10 py-10">
          <div
            class="flex flex-col items-center bg-gray-50 rounded-2xl px-10 py-14"
          >
            <h1 class="text-4xl text-color-primary-shadow font-semibold">
              Reservar habitaciones
            </h1>
            <form
              id="contactForm"
              class="flex flex-col items-center mt-5 space-y-6 w-full"
            >
              <input
                type="text"
                id="name"
                class="w-full border border-gray-300 p-2 rounded"
                placeholder="Nombre"
                required
              />
              <input
                type="tel"
                id="room"
                class="w-full border border-gray-300 p-2 rounded"
                placeholder="Tipo de habitación"
                required
              />
              <input
                type="date"
                id="date"
                class="w-full border border-gray-300 p-2 rounded"
                required
              />
              <button
                type="button"
                onclick="sendWhatsAppMessage()"
                class="bg-color-primary hover:bg-color-primary-shadow text-gray-50 font-bold py-2 px-20 rounded"
              >
                Reservar
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
    <CarouselImage />
  </div>
</section>

<script defer src="/src/components/utils/sendMessage.js"></script>